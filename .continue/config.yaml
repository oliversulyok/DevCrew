name: DevCrew Workspace Config
version: 0.1.0
models:
  - name: openrouter-nemotron
    provider: openrouter
    model: nvidia/nemotron-3-ultra-550b-a55b:free
    apiBase: https://openrouter.ai/api/v1
    # Do NOT store secrets in this file. Use an environment variable instead.
    # Set the key in your environment or use VS Code Secrets.
    apiKeyEnv: OPENROUTER_API_KEY

# Add other workspace-specific settings below as needed.
